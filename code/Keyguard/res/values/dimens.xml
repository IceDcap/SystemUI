<?xml version="1.0" encoding="utf-8"?>
<!--
/* //device/apps/common/assets/res/any/dimens.xml
**
** Copyright 2006, The Android Open Source Project
**
** Licensed under the Apache License, Version 2.0 (the "License");
** you may not use this file except in compliance with the License.
** You may obtain a copy of the License at
**
**     http://www.apache.org/licenses/LICENSE-2.0
**
** Unless required by applicable law or agreed to in writing, software
** distributed under the License is distributed on an "AS IS" BASIS,
** WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
** See the License for the specific language governing permissions and
** limitations under the License.
*/
-->
<resources>

    <!-- Size of lockscreen outerring on unsecure unlock LockScreen -->
    <dimen name="keyguard_lockscreen_outerring_diameter">270dp</dimen>

    <!-- The bottom margin for the GlowPadView container -->
    <dimen name="glowpadcontainer_bottom_margin">-80dp</dimen>

    <!-- Default target placement radius for GlowPadView. Should be 1/2 of outerring diameter. -->
    <dimen name="glowpadview_target_placement_radius">135dip</dimen>

    <!-- Default glow radius for GlowPadView -->
    <dimen name="glowpadview_glow_radius">75dip</dimen>

    <!-- Default distance beyond which GlowPadView snaps to the matching target -->
    <dimen name="glowpadview_snap_margin">40dip</dimen>

    <!-- Default distance from each snap target that GlowPadView considers a "hit" -->
    <dimen name="glowpadview_inner_radius">15dip</dimen>

    <!-- Size of status line font on Unsecure unlock LockScreen. -->
    <dimen name="keyguard_lockscreen_status_line_font_size">14dip</dimen>

    <!-- Size of right margin on Unsecure unlock LockScreen -->
    <dimen name="keyguard_lockscreen_status_line_font_right_margin">42dip</dimen>

    <!-- Size of top margin on Clock font to edge on unlock LockScreen -->
    <dimen name="keyguard_lockscreen_status_line_clockfont_top_margin">22dip</dimen>

    <!-- Size of top margin on Clock font to edge on unlock LockScreen -->
    <dimen name="keyguard_lockscreen_status_line_clockfont_bottom_margin">12dip</dimen>

    <!-- Padding on left margin of PIN text entry field to center it when del button is showing -->
    <dimen name="keyguard_lockscreen_pin_margin_left">40dip</dimen>

    <!-- Height of FaceUnlock view in keyguard -->
    <dimen name="face_unlock_height">330dip</dimen>

    <!-- Keyguard dimensions -->
    <!-- TEMP -->
    <dimen name="kg_security_panel_height">600dp</dimen>

    <!-- Height of security view in keyguard. -->
    <dimen name="kg_security_view_height">480dp</dimen>

    <!-- Width of widget view in keyguard. -->
    <dimen name="kg_widget_view_width">0dp</dimen>

    <!-- Height of widget view in keyguard. -->
    <dimen name="kg_widget_view_height">0dp</dimen>

    <!-- Size of the clock font in keyguard's status view -->
    <dimen name="kg_status_clock_font_size">75dp</dimen>

    <!-- Size of the generic status lines keyguard's status view -->
    <dimen name="kg_status_line_font_size">14sp</dimen>

    <!-- Size of margin on the right of keyguard's status view -->
    <dimen name="kg_status_line_font_right_margin">16dp</dimen>

    <!-- Top margin for the clock view -->
    <dimen name="kg_clock_top_margin">-16dp</dimen>

    <!-- Horizontal gap between keys in PIN and SIM PIN numeric keyboards in keyguard -->
    <dimen name="kg_key_horizontal_gap">0dp</dimen>

    <!-- Horizontal gap between keys in PIN and SIM PIN numeric keyboards in keyguard -->
    <dimen name="kg_key_vertical_gap">0dp</dimen>

    <!-- Horizontal gap between keys in PIN and SIM PIN numeric keyboards in keyguard -->
    <dimen name="kg_pin_key_height">60dp</dimen>

    <!-- Space reserved at the bottom of secure views (pin/pattern/password/SIM pin/SIM puk) -->
    <dimen name="kg_secure_padding_height">46dp</dimen>

    <!-- The height of the runway lights strip -->
    <dimen name="kg_runway_lights_height">7dp</dimen>

    <!-- The height of the runway lights strip -->
    <dimen name="kg_runway_lights_vertical_padding">2dp</dimen>

    <!-- Horizontal padding for the widget pager -->
    <dimen name="kg_widget_pager_horizontal_padding">16dp</dimen>

    <!-- Top padding for the widget pager -->
    <dimen name="kg_widget_pager_top_padding">0dp</dimen>

    <!-- Bottom padding for the widget pager -->
    <dimen name="kg_widget_pager_bottom_padding">64dp</dimen>

    <!--
         Top margin for the runway lights. We add a negative margin in large
        devices to account for the widget pager padding
    -->
    <dimen name="kg_runway_lights_top_margin">0dp</dimen>

    <!-- Touch slop for the global toggle accessibility gesture -->
    <dimen name="accessibility_touch_slop">80dip</dimen>

    <!-- Width of the sliding KeyguardSecurityContainer (includes 2x keyguard_security_view_margin) -->
    <dimen name="keyguard_security_width">320dp</dimen>

    <!-- Height of the sliding KeyguardSecurityContainer (includes 2x keyguard_security_view_margin) -->
    <dimen name="keyguard_security_height">400dp</dimen>
    <dimen name="zzzzz_gn_navil_secure_lock_screen_view_height">328dp</dimen>

    <!-- Max Height of the sliding KeyguardSecurityContainer (includes 2x keyguard_security_view_margin) -->
    <dimen name="keyguard_security_max_height">450dp</dimen>

    <!-- Margin around the various security views -->
    <dimen name="keyguard_security_view_margin">8dp</dimen>

    <!-- Margin around the various security views -->
    <dimen name="keyguard_muliuser_selector_margin">8dp</dimen>

    <!-- Stroke width of the frame for the circular avatars. -->
    <dimen name="keyguard_avatar_frame_stroke_width">2dp</dimen>

    <!-- Shadow radius under the frame for the circular avatars. -->
    <dimen name="keyguard_avatar_frame_shadow_radius">1dp</dimen>

    <!-- Size of the avator on hte multiuser lockscreen. -->
    <dimen name="keyguard_avatar_size">66dp</dimen>

    <!-- Size of the text under the avator on the multiuser lockscreen. -->
    <dimen name="keyguard_avatar_name_size">10sp</dimen>

    <!--
         Size of the region along the edge of the screen that will accept
         swipes to scroll the widget area.
    -->
    <dimen name="kg_edge_swipe_region_size">24dp</dimen>

    <!--
    If the height if keyguard drops below this threshold (most likely
    due to the appearance of the IME), then drop the multiuser selector.
    -->
    <dimen name="kg_squashed_layout_threshold">600dp</dimen>

    <!--
    The height of widgets which do not support vertical resizing. This is only
    used on tablets; on phones, this size is determined by the space left by the
    security mode.
    -->
    <dimen name="kg_small_widget_height">160dp</dimen>

    <!--
         EmergencyCarrierArea overlap - amount to overlap the emergency button and carrier text.
         Should be 0 on devices with plenty of room (e.g. tablets)
    -->
    <dimen name="eca_overlap">-10dip</dimen>

    <!-- Default clock parameters -->
    <dimen name="bottom_text_spacing_digital">-10dp</dimen>
    <dimen name="label_font_size">14dp</dimen>
    <dimen name="widget_label_font_size">16sp</dimen>
    <dimen name="widget_big_font_size">88dp</dimen>
    <dimen name="big_font_size">120dp</dimen>

    <!-- The y translation to apply at the start in appear animations. -->
    <dimen name="appear_y_translation_start">32dp</dimen>

    <!-- The size of the dots in the PIN unlock method. -->
    <dimen name="password_dot_size">7dp</dimen>

    <!-- The padding between chars of the password view. -->
    <dimen name="password_char_padding">8dp</dimen>

    <!-- The vertical margin between the date and the owner info. -->
    <dimen name="date_owner_info_margin">6dp</dimen>

    <!-- The translation for disappearing security views after having solved them. -->
    <dimen name="disappear_y_translation">-32dp</dimen>

    <!-- NaviKeyguard dimens begin -->

    <dimen name="kg_maincell_layout_carrier_margin_top">12dp</dimen>
    <dimen name="kg_maincell_layout_carrier_battery_gap">8dp</dimen>
    <dimen name="kg_maincell_layout_battery_notification_gap">48dp</dimen>
    <dimen name="kg_maincell_layout_notification_height">341dp</dimen>
    <dimen name="kg_cell_layout_padding">8dp</dimen>
    <dimen name="kg_exitworkspace_height">30dp</dimen>
    <dimen name="kg_infozone_fixed_panel_placeholder_height">5dp</dimen>
    <dimen name="kg_infozone_fixed_panel_height">64dp</dimen>
    <dimen name="kg_infozone_margin_bottom">10dp</dimen>
    <dimen name="kg_infozone_margin_top_at_desk">12dp</dimen>
    <dimen name="widget_left_margin">16dp</dimen>
    <dimen name="kg_infozone_height">79dp</dimen>
    <dimen name="zzzzz_gn_navil_edge_swipe_region_size">48dp</dimen>

    <!-- skylight dimens -->
    <dimen name="skylight_height">267dp</dimen>
    <dimen name="skylight_weather_height">32dp</dimen>
    <dimen name="skylight_weather_margin_bottom">23dp</dimen>
    <dimen name="skylight_weather_icon_width">32dp</dimen>
    <dimen name="skylight_weather_margin_right">32dp</dimen>
    <dimen name="skylight_time_margin_top">36dp</dimen>
    <dimen name="skylight_time_margin_top_when_no_weather">85dp</dimen>
    <dimen name="skylight_time_weather_margin_left">22dp</dimen>
    <dimen name="skylight_time_date_margin">0dp</dimen>
    <dimen name="skylight_time_week_margin_left">7dp</dimen>
    <dimen name="skylight_weathericon_temp_margin">4dp</dimen>
    <dimen name="skylight_temp_weather_margin">20dp</dimen>
    <dimen name="skylight_miss_icon_margin_right">20dp</dimen>
    <dimen name="skylight_miss_icon_margin_top">67dp</dimen>
    <dimen name="skylight_miss_call_sms_margin">22dp</dimen>
    <dimen name="skylight_miss_icon_width_height">18dp</dimen>
    <!-- 天窗左右安全距离 -->
    <dimen name="skylight_safe_margin_left">26dp</dimen>
    <dimen name="skylight_safe_margin_right">26dp</dimen>
    <dimen name="skylight_music_widget_margin_top">69dp</dimen>
    <dimen name="skylight_music_padding_bottom">10dp</dimen>
    <dimen name="skylight_time_text_size">67dp</dimen>
    <dimen name="skylight_date_week_text_size">15dp</dimen>
    <dimen name="skylight_weather_text_size">14dp</dimen>
    <dimen name="skylight_time_am_pm_cn_text_size">23dp</dimen>
    <dimen name="skylight_time_am_pm_en_text_size">20dp</dimen>

    <!-- skylight dimens -->


    <!-- notification dimens start -->
    <!-- Height of a large notification in the status bar -->
    <dimen name="notification_max_height">256dp</dimen>
    <!-- Height of a small notification in the status bar -->
    <dimen name="notification_min_height">64dp</dimen>
    <!-- radius of the corners of the material rounded rect background -->
    <dimen name="notification_material_rounded_rect_radius">2dp</dimen>
    <!-- The size of the gesture span needed to activate the "pull" notification expansion -->
    <dimen name="pull_span_min">25dp</dimen>
    <!-- Falsing threshold used when dismissing notifications from the lockscreen. -->
    <dimen name="swipe_helper_falsing_threshold">70dp</dimen>
    <!-- Height of notification icons in the status bar -->
    <dimen name="status_bar_icon_size">@*android:dimen/status_bar_icon_size</dimen>
    <!-- size at which Notification icons will be drawn in the status bar -->
    <dimen name="status_bar_icon_drawing_size">17dip</dimen>
    <!--
         Additional translation (downwards) for appearing notifications when going to the full shade
         from Keyguard.
    -->
    <dimen name="go_to_full_shade_appearing_translation">200dp</dimen>
    <!-- The top padding of the clear all button -->
    <dimen name="clear_all_padding_top">12dp</dimen>
    <!-- The padding between the individual notification cards when dimmed. -->
    <dimen name="notification_padding_dimmed">0dp</dimen>
    <!-- The padding between the individual notification cards. -->
    <dimen name="notification_padding">3dp</dimen>
    <!-- Space reserved for the cards behind the top card in the top stack -->
    <dimen name="top_stack_peek_amount">0dp</dimen>
    <!-- Space reserved for the cards behind the top card in the bottom stack -->
    <dimen name="bottom_stack_peek_amount">0dp</dimen>
    <!-- Z distance between notifications if they are in the stack -->
    <dimen name="z_distance_between_notifications">1dp</dimen>
    <!-- The height of the area before the bottom stack in which the notifications slow down -->
    <dimen name="bottom_stack_slow_down_length">8dp</dimen>
    <!-- The height of the area before the top stack in which the notifications slow down -->
    <dimen name="top_stack_slow_down_length">8dp</dimen>
    <!-- The padding to the second card when the notifications collapse. -->
    <dimen name="notification_collapse_second_card_padding">8dp</dimen>
    <!-- The minimum amount of top overscroll to go to the quick settings. -->
    <dimen name="min_top_overscroll_to_qs">36dp</dimen>
    <dimen name="kg_maincell_layout_padding_left_and_right">10dp</dimen>
    <!-- notification dimens end -->

    <dimen name="complex_password_textview_margintop">70dp</dimen>
    <dimen name="complex_password_emergency_button_rel_height">56dp</dimen>
    <dimen name="complex_password_linearLayout_height">36dp</dimen>
    <dimen name="complex_password_linearLayout_margintop">20dp</dimen>
    <dimen name="double_tap_area_radius">83dp</dimen>

    <!-- NaviKeyguard dimens end -->

</resources>